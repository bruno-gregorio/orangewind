<script lang="ts">
  import { withBase } from '$lib/paths'
</script>

<h2 id="overview" class="ow-heading-2">Overview</h2>
<p>
  Orangewind re-implements Canonical's Vanilla Framework as a Tailwind CSS v4
  plugin, reusing Tailwind's native utilities instead of duplicating them. This
  documentation site is built with Orangewind itself, so every page is also a
  demonstration of the framework in use.
</p>

<h2 id="navigating" class="ow-heading-2">Navigating these docs</h2>
<p>
  Use the side navigation to browse components by category. Each component page
  opens with a live demonstration, followed by an explanation of its markup and
  the variants it supports.
</p>

<div class="ow-notification-information">
  <div class="ow-notification-content">
    <p class="ow-notification-message">
      Installation instructions will be added once Orangewind is published to
      npm. For now, explore the components below.
    </p>
  </div>
</div>

<h2 id="start" class="ow-heading-2">Where to start</h2>
<p>
  The <a href={withBase('/docs/components/buttons')}>Buttons</a> page is a good first
  stop — it shows the example format used throughout these docs.
</p>
